extends Node2D

func _process(delta):
	pass
#	for i in range(get_children().size()):
#		var child = get_children()[i]
#
#		if child.transform.origin.y > $Player.transform.origin.y:
#			move_child(child, $Player.get_index() + 1)
#		else:
#			move_child(child, $Player.get_index() - 1)
	
#	for child in get_children():
#		if not child.is_in_group('ysort'): continue
#
#		if child.transform.origin.y > $"Player".transform.origin.y:
#			move_child()
#			child.z_index = $"Player".z_index + 1
#		else:
#			child.z_index = $"Player".z_index - 1
